<h2>Sprints</h2>
<br />
<table class="bordered-table zebra-striped">
  <tr>
    <th>Number</th>
    <th>Start date</th>
    <th>End date</th>
    <th>Velocity</th>
    <th></th>
    <th></th>
  </tr>

<% @sprints.each do |sprint| %>
  <tr>
    <td><%= sprint.number %></td>
    <td><%= sprint.start_date %></td>
    <td><%= sprint.end_date %></td>
    <td><%= sprint.velocity %></td>
    <td><%= button_to 'Show', sprint, method: :get, class: "btn" %></td>
    <td><%= button_to 'Edit', edit_sprint_path(sprint), method: :get, class: "btn" %></td>
  </tr>
<% end %>
</table>

<br />

<%= button_to 'New Sprint', new_sprint_path, method: :get, class: "btn success" %>

